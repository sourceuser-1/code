from setuptools import setup

setup(
   name='drl',
   version='0.1',
   description='DRL Reinforcement Learning Package',
   packages=['drl'],
   install_requires=[]#'tqdm', 'numpy', 'gym', 'tensorflow', 'pyglet', 'box2d-py']
)
